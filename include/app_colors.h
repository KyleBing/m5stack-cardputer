#pragma once

#include "M5Cardputer.h"

// 菜单触发键字母色（按键块背景）
static constexpr uint16_t APP_COLOR_MENU_KEY = YELLOW;

// Info / label:value
static constexpr uint16_t APP_COLOR_LABEL = CYAN;
static constexpr uint16_t APP_COLOR_VALUE = WHITE;

// 正文 / 标题
static constexpr uint16_t APP_COLOR_TEXT = WHITE;

// 提示 / 次要
static constexpr uint16_t APP_COLOR_HINT = LIGHTGREY;
static constexpr uint16_t APP_COLOR_MUTED = DARKGREY;

// 状态
static constexpr uint16_t APP_COLOR_WARN = ORANGE;
static constexpr uint16_t APP_COLOR_ERROR = RED;
static constexpr uint16_t APP_COLOR_OK = GREEN;

// 按键块前景
static constexpr uint16_t APP_COLOR_KEY_TEXT = BLACK;
