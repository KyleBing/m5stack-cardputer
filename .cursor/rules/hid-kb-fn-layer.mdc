---
description: HID Keyboard app Fn layer mappings and usage conventions
globs: src/app_hid_kb.cpp,include/app_hid_kb.h
alwaysApply: false
---

# HID Keyboard（`h` 应用）Fn 层约定

库不会自动做 Fn 映射；`app_hid_kb.cpp` 在组 HID report 时自行转换。

## 面板橙色 / 功能键

| 组合 | HID |
|------|-----|
| Fn + \` | Esc |
| Fn + Bksp | Delete |
| Fn + ; , . / | ↑ ← ↓ → |
| Fn + 1..0 | F1..F10 |
| Fn + - / = | F11 / F12 |

未列出的 Fn+键：吞掉，不发给主机。

## 左下角修饰键（Ctrl / Opt / Alt）

| 组合 | 含义 |
|------|------|
| Ctrl / Opt / Alt | Left Ctrl / Left GUI(Win·Cmd) / Left Alt |
| Fn + Ctrl / Opt / Alt | Right Ctrl / Right GUI / Right Alt |
| Fn + Aa(Shift) | Caps Lock（并翻转本地 caps 状态） |

## 应用热键（不发给主机）

| 组合 | 作用 |
|------|------|
| Fn + u / b | USB / BLE |
| Fn + p | BLE 重新配对 |
| Fn + h | 帮助页（`,` / `.` 翻页，`h` 关闭） |
| BtnA | 退出应用（按键都给主机，不能用 Esc 退出） |

## 注意事项

- 默认 BLE，避免占用 USB 烧录口；USB 模式退出后需恢复 Serial/JTAG
- 帮助文案与映射表变更时，同步改 `drawHelpPage()` 三页内容
